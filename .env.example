# .env.example — copy to .env and fill in values

# Database
DATABASE_URL="postgresql://ramadan:ramadan_secret@localhost:5432/ramadan_tracker"
POSTGRES_USER="ramadan"
POSTGRES_PASSWORD="ramadan_secret"
POSTGRES_DB="ramadan_tracker"

# Auth — MUST CHANGE IN PRODUCTION (min 32 chars, random string)
JWT_SECRET="your-super-secret-jwt-key-at-least-32-chars-long"

# App URL
NEXTAUTH_URL="http://localhost:3000"

# Node environment
NODE_ENV="development"
